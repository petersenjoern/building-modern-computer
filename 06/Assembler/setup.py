from setuptools import setup

setup(
   name='assembler',
   version='1.0',
   description='Assembler asm to hack',
   author='xxx',
   author_email='xx@protonmail.com',
   packages=['assembler']  #same as name
)